BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[attendances] ADD [realPunch] BIT CONSTRAINT [attendances_realPunch_df] DEFAULT 1;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
