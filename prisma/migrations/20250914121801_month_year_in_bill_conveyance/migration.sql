BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[bills] ADD [month] NVARCHAR(1000),
[year] NVARCHAR(1000);

-- AlterTable
ALTER TABLE [dbo].[conveyances] ADD [month] NVARCHAR(1000),
[year] NVARCHAR(1000);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
