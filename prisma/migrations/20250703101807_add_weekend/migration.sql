BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[locations] ADD [weekend] NVARCHAR(1000) NOT NULL CONSTRAINT [locations_weekend_df] DEFAULT 'Friday';

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
