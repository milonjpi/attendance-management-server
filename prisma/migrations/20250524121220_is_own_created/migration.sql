BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[employees] ADD [isOwn] BIT NOT NULL CONSTRAINT [employees_isOwn_df] DEFAULT 0;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
