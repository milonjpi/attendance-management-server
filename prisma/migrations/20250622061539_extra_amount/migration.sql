BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[conveyances] ADD [extraAmount] INT NOT NULL CONSTRAINT [conveyances_extraAmount_df] DEFAULT 0;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
